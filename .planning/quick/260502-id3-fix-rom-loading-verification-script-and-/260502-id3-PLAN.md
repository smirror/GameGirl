---
quick_id: 260502-id3
status: complete
date: 2026-05-02
---

# Quick Task 260502-id3: Fix ROM loading verification script and CI failure for cartridge type coverage

## Goal

Make the CI ROM-loading verification pass for the checked-in ROM corpus, and expose the same verification as a local shell command.

## Tasks

1. Update cartridge metadata parsing so known Game Boy cartridge type codes in the ROM corpus are recognized instead of rejected as unsupported.
2. Add a reusable shell script for verifying all checked-in `.gb` and `.gbc` files.
3. Update `.github/workflows/rust.yml` to call the script, then verify with format, tests, and local ROM loading.
