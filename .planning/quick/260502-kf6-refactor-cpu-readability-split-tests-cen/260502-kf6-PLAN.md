---
quick_id: 260502-kf6
status: planned
created: 2026-05-02
---

# Quick Task 260502-kf6: Refactor CPU readability, split tests, centralize memory map constants, and clean ROM loading API

## Goal

Apply the requested readability improvements without changing emulator behavior.

## Tasks

1. Split CPU implementation and tests.
   - Move the large inline CPU test module into `src/cpu/tests.rs`.
   - Split `Cpu::step` into instruction-family helpers while keeping the current simple match style.
   - Add a small PC-advance helper for non-branching instructions.

2. Tighten invariants and shared constants.
   - Make the flag register field private behind existing setter/getter methods.
   - Add shared memory-map constants and use them in Bus, Cartridge, CPU, and tests where practical.

3. Clean ROM loading API and verify.
   - Make `load_rom_file` clone-free and route CLI file reads through it.
   - Run formatting, tests, and clippy.

